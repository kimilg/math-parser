version: "2"
sql:
  - engine: "postgresql"
    queries: "db/query"
    schema: "migrations"
    database:
      uri: "postgres://postgres:dlfrn30@localhost:5432/math"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"